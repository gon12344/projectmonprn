<UserControl x:Class="WpfApp1.Views.CourseManagementView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
 
    <Grid Margin="10">
  <Grid.RowDefinitions>
   <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
  <RowDefinition Height="Auto"/>
   <RowDefinition Height="*"/>
  </Grid.RowDefinitions>
    
   <!-- CRUD Buttons -->
<StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
 <Button Content="+ Add Course" Command="{Binding AddCourseAsyncCommand}" 
 Width="120" Height="35" Margin="0,0,10,0" Background="LightGreen"/>
   <Button Content="* Edit Course" Command="{Binding EditCourseAsyncCommand}" 
 Width="120" Height="35" Margin="0,0,10,0" Background="LightBlue"/>
 <Button Content="- Delete Course" Command="{Binding DeleteCourseCommand}" 
Width="120" Height="35" Margin="0,0,10,0" Background="LightCoral"/>
    <Button Content="^ Refresh" Command="{Binding RefreshCommand}" 
  Width="120" Height="35" Margin="0,0,10,0" Background="LightYellow"/>
</StackPanel>

    <!-- Search Box -->
 <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
  <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
<TextBox Width="300" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
  VerticalAlignment="Center" Margin="0,0,10,0" Height="30" 
   BorderThickness="2" BorderBrush="SteelBlue"/>
   <Button Content="Search" Command="{Binding SearchCommand}" Width="70" Height="30" 
Background="SteelBlue" Foreground="White"/>
</StackPanel>

  <!-- Input Form -->
 <Border Grid.Row="2" Background="AliceBlue" Padding="15" Margin="0,0,0,15" 
 CornerRadius="5" BorderBrush="Blue" BorderThickness="2">
  <Grid>
 <Grid.RowDefinitions>
 <RowDefinition Height="Auto"/>
    <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
     <Grid.ColumnDefinitions>
   <ColumnDefinition Width="Auto"/>
   <ColumnDefinition Width="200"/>
<ColumnDefinition Width="Auto"/>
   <ColumnDefinition Width="200"/>
    <ColumnDefinition Width="Auto"/>
 <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

   <!-- Row 1 -->
  <TextBlock Grid.Row="0" Grid.Column="0" Text="Course Code:" VerticalAlignment="Center" 
Margin="0,0,10,8" FontWeight="SemiBold"/>
<TextBox Grid.Row="0" Grid.Column="1" Height="28" 
  Margin="0,0,15,8" ToolTip="Enter course code"
   Text="{Binding InputCourseCode, UpdateSourceTrigger=PropertyChanged}"/>

     <TextBlock Grid.Row="0" Grid.Column="2" Text="Course Name:" VerticalAlignment="Center" 
Margin="0,0,10,8" FontWeight="SemiBold"/>
<TextBox Grid.Row="0" Grid.Column="3" Height="28" 
Margin="0,0,15,8" ToolTip="Enter course name"
        Text="{Binding InputCourseName, UpdateSourceTrigger=PropertyChanged}"/>

 <TextBlock Grid.Row="0" Grid.Column="4" Text="Credits:" VerticalAlignment="Center" 
      Margin="0,0,10,8" FontWeight="SemiBold"/>
<TextBox Grid.Row="0" Grid.Column="5" Height="28" 
 Margin="0,0,10,8" ToolTip="Enter number of credits"
  Text="{Binding InputCredits, UpdateSourceTrigger=PropertyChanged}"/>

     <!-- Row 2 -->
  <TextBlock Grid.Row="1" Grid.Column="0" Text="Department:" VerticalAlignment="Center" 
   Margin="0,0,10,8" FontWeight="SemiBold"/>
   <TextBox Grid.Row="1" Grid.Column="1" Height="28" 
 Margin="0,0,15,8" ToolTip="Enter department name"
        Text="{Binding InputDepartment, UpdateSourceTrigger=PropertyChanged}"/>

  <TextBlock Grid.Row="1" Grid.Column="2" Text="Description:" VerticalAlignment="Center" 
 Margin="0,0,10,8" FontWeight="SemiBold"/>
  <TextBox Grid.Row="1" Grid.Column="3" Height="28" 
Margin="0,0,15,8" ToolTip="Enter course description"
        Text="{Binding InputDescription, UpdateSourceTrigger=PropertyChanged}"/>

    <CheckBox Grid.Row="1" Grid.Column="4" Grid.ColumnSpan="2"
     Content="Course is active" Margin="0,0,10,8" 
      FontWeight="SemiBold" IsChecked="{Binding InputIsActive, Mode=TwoWay}"/>
      </Grid>
    </Border>
  
  <!-- Course DataGrid -->
 <DataGrid Grid.Row="3" ItemsSource="{Binding FilteredCourses}" SelectedItem="{Binding SelectedCourse}"
 AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
   GridLinesVisibility="Horizontal" RowHeaderWidth="0" 
 AlternatingRowBackground="LightGray" SelectionMode="Single"
    SelectionChanged="DataGrid_SelectionChanged">
<DataGrid.Columns>
<DataGridTextColumn Header="Course Code" Binding="{Binding CourseCode}" Width="100" 
    FontWeight="Bold"/>
     <DataGridTextColumn Header="Course Name" Binding="{Binding CourseName}" Width="250"/>
 <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="80"/>
<DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="150"/>
  <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="100"/>
   <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="300"/>
   </DataGrid.Columns>
  </DataGrid>
 </Grid>
</UserControl>