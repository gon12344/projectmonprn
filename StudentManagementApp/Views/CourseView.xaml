<UserControl x:Class="WpfApp1.Views.CourseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <StackPanel Margin="0,0,20,0">
            <TextBlock Text="Course Code"/>
            <TextBox Text="{Binding CourseCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10"/>
            <TextBlock Text="Course Name"/>
            <TextBox Text="{Binding CourseName, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10"/>
            <TextBlock Text="Description"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10" Height="60" AcceptsReturn="True"/>
            <TextBlock Text="Credits"/>
            <TextBox Text="{Binding Credits, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10"/>
            <TextBlock Text="Department"/>
            <TextBox Text="{Binding Department, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,10"/>
            <CheckBox Content="Is Active" IsChecked="{Binding IsActive}"/>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="New" Command="{Binding NewCourseCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Content="Save" Command="{Binding SaveCourseCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Content="Edit" Command="{Binding EditCourseCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Content="Delete" Command="{Binding DeleteCourseCommand}" Width="80"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Column="1">
            <DockPanel>
                <TextBox Width="250" Margin="0,0,10,10" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Search" Command="{Binding SearchCoursesCommand}" Width="80" Margin="0,0,10,0"/>
                <Button Content="Clear" Command="{Binding ClearSearchCommand}" Width="80"/>
            </DockPanel>
            <DataGrid ItemsSource="{Binding Courses}" SelectedItem="{Binding SelectedCourse}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Code" Binding="{Binding CourseCode}" Width="*"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding CourseName}" Width="2*"/>
                    <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="*"/>
                    <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
