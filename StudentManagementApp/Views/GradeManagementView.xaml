<UserControl x:Class="WpfApp1.Views.GradeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBlock Text="Select Course" FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding Courses}"
                       SelectedItem="{Binding SelectedCourse}"
                      DisplayMemberPath="CourseName"
                      Margin="0,4,0,10" />

            <TextBlock Text="Enrolled Students" FontWeight="Bold" Margin="0,10,0,4" />
            <DataGrid ItemsSource="{Binding EnrolledStudents}"
                      SelectedItem="{Binding SelectedEnrollment}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      Height="450">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Student ID" Binding="{Binding Student.StudentCode}" Width="*" />
                    <DataGridTextColumn Header="Full Name" Binding="{Binding Student.FullName}" Width="2*" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" />
                    <DataGridTextColumn Header="Grade" Binding="{Binding Grade}" Width="*" />
                    <DataGridTextColumn Header="Graded Date" Binding="{Binding GradedDate, StringFormat={}{0:yyyy-MM-dd}}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>

        <StackPanel Grid.Column="1" Margin="10,0,0,0">
            <Border BorderBrush="Gray" BorderThickness="1" Padding="10">
                <StackPanel>
                    <TextBlock Text="Update Grade" FontSize="16" FontWeight="Bold" />

                    <TextBlock Text="Selected Student:" Margin="0,10,0,0" />
                    <TextBlock Text="{Binding SelectedEnrollment.Student.FullName, FallbackValue='(No student selected)'}"
                                FontWeight="Bold" />

                    <TextBlock Text="Enter Grade (0-10):" Margin="0,10,0,4" />
                    <TextBox Text="{Binding CurrentGrade, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />

                    <Button Content="Save Grade" 
                             Command="{Binding UpdateGradeCommand}" 
                             IsEnabled="{Binding IsNotBusy}" 
                             Width="120" />
                </StackPanel>
            </Border>

            <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="Course Statistics" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Average Grade:" FontWeight="SemiBold" Width="100" />
                        <TextBlock Text="{Binding CourseAverage}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Highest Grade:" FontWeight="SemiBold" Width="100" />
                        <TextBlock Text="{Binding CourseMax}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Lowest Grade:" FontWeight="SemiBold" Width="100" />
                        <TextBlock Text="{Binding CourseMin}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
